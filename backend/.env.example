# Copy this to .env on each machine with local overrides
PORT=5000
MONGO_URI=your_mongodb_connection_string
JWT_SECRET=change_me_for_dev

# SMTP (để gửi email thật). Ví dụ cấu hình Gmail SMTP (cần App Password, không dùng mật khẩu thường):
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=465
# SMTP_USER=your@gmail.com
# SMTP_PASS=your_app_password
# SMTP_FROM="Taskly <your@gmail.com>"

# URL frontend (dùng để sinh link đặt lại trong email)
FRONTEND_BASE_URL=http://localhost:8081


RESEND_API_KEY=re_9oa9obh5_98HXZbo6rb85nLxYSUTxtReM
